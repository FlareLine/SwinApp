<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="SwinApp.AnnouncementPage"
             Title="Blackboard Announcements">
    <StackLayout Padding="5,0">
        <ListView x:Name="ListAnnouncementTime" HasUnevenRows="True">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <Grid Padding="0">
                            <StackLayout Margin="5">
                                <Label Text="{Binding Title}" FontAttributes="Bold"/>
                                <Label Text="{Binding Created, StringFormat='{0:dd/MM/yyyy}'}"/>
                            </StackLayout>
                        </Grid>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <Label x:Name="LabelPickerAnnouncementFilter">Filter by...</Label>
        <Picker x:Name="PickerAnnouncementFilter"/>
    </StackLayout>
</ContentPage>