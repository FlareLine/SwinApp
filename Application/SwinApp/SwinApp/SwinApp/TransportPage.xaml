<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
			 xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
			 x:Class="SwinApp.TransportPage"
			 Title="Transport Guide">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="4*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <StackLayout x:Name="TransportStack" VerticalOptions="FillAndExpand" HorizontalOptions="Center" Grid.Column="1">
            <ListView x:Name="TransportList" RowHeight="50" HasUnevenRows="True" SeparatorVisibility="None" Margin="0">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <Grid Padding="0">
                                <Frame Margin="5">
                                    <StackLayout Orientation="Vertical" Padding="1">
                                        <ContentView Content="{Binding Content}" />
                                    </StackLayout>
                                </Frame>
                            </Grid>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </StackLayout>
    </Grid>
</ContentPage>